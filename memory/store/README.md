# Runtime Memory Store

This directory stores local JSONL files written by the approval and feedback workflows.

Generated files should be treated as local runtime state, not committed source assets. The directory is retained in the repository so local workflow runs have a predictable path.
