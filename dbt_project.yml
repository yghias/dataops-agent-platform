name: dataops_agent_platform
version: 1.0.0
config-version: 2

profile: dataops_agent_platform

model-paths: ["models"]
seed-paths: ["sample_data"]
macro-paths: []
test-paths: ["models/tests"]
snapshot-paths: []

models:
  dataops_agent_platform:
    staging:
      +materialized: view
      +schema: staging
    intermediate:
      +materialized: table
      +schema: intermediate
    marts:
      +materialized: table
      +schema: analytics
